version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: pingerbot
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pingerbotdb
    networks:
      - default

  bot:
    image: pingerbot:latest
    restart: always
    networks:
      - default